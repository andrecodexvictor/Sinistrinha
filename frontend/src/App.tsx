// This file is intentionally minimal — the real app entry point is main.tsx
// which sets up the React Router with all pages and layouts.
//
// This component is kept only for backwards compatibility with any tooling
// that expects a default App export.

export default function App() {
  return null;
}
